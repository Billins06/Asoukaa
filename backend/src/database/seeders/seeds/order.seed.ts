import { DataSource } from 'typeorm';

export async function seedOrders(dataSource: DataSource) {
  console.log('⏭️  Orders seeding skipped (complex structure - seed manually if needed)');
  return;
}
